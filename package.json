{
  "name": "edith-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "prettier": "^3.0.0",
    "eslint": "^8.0.0"
  },
  "packageManager": "npm@8.6.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}